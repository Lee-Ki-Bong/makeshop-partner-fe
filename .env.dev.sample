# ============================
# 브라우저 노출 전용 (Client Runtime)
# ============================
NEXT_PUBLIC_APP_ENV=dev
NEXT_PUBLIC_API_BASE_URL=https://partner-dev.makeshop.co.kr/api

# ============================
# 서버 / 도구 전용 (Node / Orval / CI)
# ============================
APP_ENV=dev

# OpenAPI 원격 스펙 URL
OPENAPI_URL=https://partner-dev.makeshop.co.kr/openapi.json

# Orval 동작 모드
# - snapshot : 로컬 스냅샷 사용 (기본)
# - remote   : 원격 OpenAPI 직접 사용
ORVAL_SOURCE=snapshot

# OpenAPI 스펙 스냅샷 경로
OPENAPI_SNAPSHOT_PATH=api/openapi.json

# Orval 생성 코드 출력 디렉토리
ORVAL_OUTPUT_DIR=api/generated
ORVAL_SCHEMA_DIR=api/generated/model
